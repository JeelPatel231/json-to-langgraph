[project]
name = "workflow-dsl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "common-expression-language>=0.5.6",
    "grandalf>=0.8",
    "langgraph>=1.1.10",
    "pydantic>=2.13.4",
]
